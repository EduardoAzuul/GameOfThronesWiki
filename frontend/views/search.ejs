<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- The title will dynamically change based on the character's name -->
    <title>Game of Thrones Encyclopedia: <%= character.fullName %></title>
    <!-- Basic styling to make the character viewer look good -->
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            background-color: #1a1a1a;
            color: #e0e0e0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            padding: 20px;
            box-sizing: border-box;
        }
        .container {
            background-color: #2c2c2c;
            border-radius: 12px;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.5);
            overflow: hidden;
            width: 100%;
            max-width: 400px;
            text-align: center;
        }
        .character-card {
            padding: 25px;
        }
        .character-image {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            border: 4px solid #4a4a4a;
            object-fit: cover;
            margin-bottom: 20px;
            background-color: #444; /* Placeholder background */
        }
        h1 {
            margin: 0;
            font-size: 1.8em;
            color: #ffffff;
        }
        h2 {
            margin: 5px 0 15px;
            font-size: 1.1em;
            color: #b0b0b0;
            font-weight: normal;
        }
        .details {
            text-align: left;
            margin-bottom: 20px;
            line-height: 1.6;
        }
        .details p {
            margin: 8px 0;
            background-color: #383838;
            padding: 10px;
            border-radius: 6px;
        }
        .details strong {
            color: #dcdcdc;
        }
        .navigation, .search-form {
            padding: 20px;
            background-color: #333333;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .search-form {
             border-top: 1px solid #444;
             gap: 10px;
        }
        .nav-btn, .search-btn {
            background-color: #555;
            color: white;
            border: none;
            padding: 12px 20px;
            border-radius: 8px;
            cursor: pointer;
            text-decoration: none;
            font-size: 1em;
            transition: background-color 0.3s;
        }
        .nav-btn:hover, .search-btn:hover {
            background-color: #007bff;
        }
        .search-input {
            flex-grow: 1;
            padding: 12px;
            border-radius: 8px;
            border: 1px solid #4a4a4a;
            background-color: #2c2c2c;
            color: #e0e0e0;
            font-size: 1em;
        }
        .counter {
            width: 100%;
            text-align: center;
            padding-bottom: 15px;
            color: #888;
        }
    </style>
</head>
<body>

    <div class="container">
        <!-- This section will display the main character information -->
        <div class="character-card">
            
            <!-- EJS checks if an image URL exists. If not, it shows a placeholder -->
            <% if (character.imageUrl) { %>
                <img src="<%= character.imageUrl %>" alt="Image of <%= character.fullName %>" class="character-image">
            <% } else { %>
                <!-- Placeholder for characters without an image -->
                <div class="character-image" style="display: flex; justify-content: center; align-items: center; font-size: 3em;">?</div>
            <% } %>

            <h1><%= character.fullName %></h1>
            <h2><%= character.title %></h2>
            
            <div class="details">
                <p><strong>Family:</strong> <%= character.family %></p>
                <!-- Only display 'Born' if the data is available -->
                <% if (character.born) { %>
                    <p><strong>Born:</strong> <%= character.born %></p>
                <% } %>
                <!-- Only display 'Died' if the data is available -->
                <% if (character.died) { %>
                    <p><strong>Died:</strong> <%= character.died %></p>
                <% } %>
                <!-- Check if the aliases array is not empty before displaying it -->
                <% if (character.aliases && character.aliases.length > 0) { %>
                    <p><strong>Aliases:</strong> <%= character.aliases.join(', ') %></p>
                <% } %>
            </div>

             <!-- Shows the current character's position, e.g., "5 of 53" -->
            <div class="counter">
                <%= currentIndex + 1 %> of <%= totalCharacters %>
            </div>
        </div>

        <!-- Navigation buttons to go to the previous/next character -->
        <div class="navigation">
            <!-- The links use the currentIndex to calculate the next index -->
            <a href="/character/<%= currentIndex - 1 %>" class="nav-btn">Previous</a>
            <a href="/character/<%= currentIndex + 1 %>" class="nav-btn">Next</a>
        </div>

        <!-- Search form to find a character by name -->
        <!-- It sends a POST request to the /search route defined in index.js -->
        <form action="/search" method="POST" class="search-form">
            <input type="text" name="characterName" class="search-input" placeholder="Find a character..." required>
            <button type="submit" class="search-btn">Search</button>
        </form>
    </div>

</body>
</html>

