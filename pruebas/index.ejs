<!DOCTYPE html>
<html lang="es" data-bs-theme="dark">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Enciclopedia de Hielo y Fuego: <%= character ? character.fullName : 'Explorar' %></title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;700&family=Lato:wght@300;400;700&display=swap" rel="stylesheet">
        <!-- Enlace a tu CSS en la carpeta public/css -->
        <link rel="stylesheet" href="/css/style.css">
    </head>
    
    <body>
        <div class="container my-5">
            <header class="text-center mb-5">
                <h1 class="display-4 titulo-enciclopedia">Enciclopedia de Hielo y Fuego</h1>
            </header>

            <form id="search-form" class="search-form mb-5">
                <div class="input-group">
                    <input type="text" name="characterName" class="form-control form-control-lg search-input" placeholder="Busca por el nombre de un personaje..." required>
                    <button type="submit" class="btn btn-buscar">Valar Morghulis</button>
                </div>
            </form>

            <% if (character) { %>
                <!-- MUESTRA UN SOLO PERSONAJE (RESULTADO DE BÚSQUEDA) -->
                <div class="character-card p-0 mb-4">
                    <div class="row g-0">
                        <div class="col-md-4">
                            <% if (character.image) { %>
                                <img src="<%= character.image %>" alt="Imagen de <%= character.fullName %>" class="character-image">
                            <% } else { %>
                                <div class="image-placeholder">?</div>
                            <% } %>
                        </div>
                        <div class="col-md-8 d-flex flex-column">
                            <div class="card-body p-4">
                                <h1 class="display-5 mb-0 character-name"><%= character.fullName %></h1>
                                <p class="mb-4 character-title"><%= character.titles %></p>
                                <div class="details mb-4">
                                    <p><strong>Familia:</strong> <%= character.family %></p>
                                    <p><strong>Nacimiento:</strong> <%= character.born %></p>
                                    <p><strong>Muerte:</strong> <%= character.died %></p>
                                    <p><strong>Alias:</strong> <%= character.aliases %></p>
                                </div>
                                <div class="text-center">
                                    <span class="counter"><%= character.id %> de <%= totalCharacters %></span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="navigation d-flex justify-content-between" data-character-id="<%= character.id %>">
                    <button id="prev-btn" class="nav-btn rounded-pill">Anterior</button>
                    <button id="next-btn" class="nav-btn rounded-pill">Siguiente</button>
                </div>

            <% } else if (allCharacters && allCharacters.length > 0) { %>
                <!-- MUESTRA LA GALERÍA DE TODOS LOS PERSONAJES AL INICIO -->
                <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 row-cols-lg-4 g-4">
                    <% allCharacters.forEach(char => { %>
                        <div class="col">
                            <a href="/character/<%= char.id %>" class="text-decoration-none">
                                <div class="card character-gallery-card h-100">
                                    <img src="<%= char.image || 'https://placehold.co/400x250/1c1c1c/D4AF37?text=?' %>" class="card-img-top gallery-img" alt="Imagen de <%= char.fullName %>">
                                    <div class="card-body text-center">
                                        <h5 class="card-title gallery-title"><%= char.fullName %></h5>
                                    </div>
                                </div>
                            </a>
                        </div>
                    <% }); %>
                </div>

            <% } else { %>
                <!-- MUESTRA MENSAJE DE BIENVENIDA O ERROR -->
                <div class="text-center">
                    <% if (error) { %>
                        <div class="alert alert-danger welcome-message"><h2 class="display-6"><%= error %></h2></div>
                    <% } else { %>
                        <div class="welcome-message">
                            <h2 class="display-6 text-muted">La noche es oscura y alberga horrores...</h2>
                            <p class="lead">Usa el buscador para encontrar a los personajes de la saga.</p>
                        </div>
                    <% } %>
                </div>
            <% } %>
        </div>

        <!-- Enlace a tu JS en la carpeta public/js -->
        <script src="/js/main.js"></script>
    </body>
</html>
